@model ConcertListViewModel //pobiera medoel koncert

<h1>@Model.Title</h1>

<div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
    @foreach (var concert in Model.Concerts)
    {
        <partial name="_ConcertCard" model="concert" />
    }
</div>