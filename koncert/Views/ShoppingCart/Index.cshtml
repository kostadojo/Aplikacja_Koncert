@model ShoppingCartViewModel

<h3 class="my-5">
    Twoje zamówienie
</h3>
<div class="row gx-3">
    <div class="col-8">
        @foreach (var item in Model.ShoppingCart.ShoppingCartItems)
        {
            <div class="card shopping-cart-card mb-2">
                <div class="row">
                    <div class="col-md-4">
                        <img src="@item.concert.ImageThumbnailUrl" class="img-fluid rounded-start p-2" alt="@item.concert.Name">
                    </div>
                    <div class="col-md-8">
                        <div class="card-body">
                            <h5 class="card-text">@item.Amount x @item.concert.Name</h5>
                            <div class="d-flex justify-content-between">
                                <h6>@item.concert.Artist</h6>
                                <h2>@item.concert.Price.ToString("c")</h2>
                            </div>
                            <ul>
                                @foreach (var it in item.concert.Description.NoteOneDescription)
                                {
                                    <li>@it.Content</li>
                                }
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
    <div class="col-4">
        <div class="card shopping-cart-card p-3">
            <div class="row">
                <h4 class="col">Wartość Koszyka:</h4>
                <h4 class="col text-end">@Model.ShoppingCartTotal.ToString("c")</h4>
            </div>
            <hr />
            <div class="text-center d-grid">
                <a class="btn btn-secondary"
                   asp-controller="Order"
                   asp-action="Checkout">
                    <h4>Zamawiam</h4>
                </a>
            </div>
        </div>
    </div>
</div>
